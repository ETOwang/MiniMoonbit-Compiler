/// Preprocesses the syntax tree, extracting the contents of functions `main` and `init` to top-level.
pub fn syntax_preprocess(
  self : KnfEnv,
  s : @types.Syntax
) -> @types.Syntax {
  abort("todo")
}
